% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot-scm.R
\name{scm_from_df}
\alias{scm_from_df}
\title{Build an SCM from a wide data frame}
\usage{
scm_from_df(data, K_min = 3)
}
\arguments{
\item{data}{A data.frame or tibble with columns following LMTP naming}

\item{K_min}{Minimum number of waves (default 3)}
}
\value{
Object of class \code{scm}
}
\description{
Creates an SCM object from a wide data frame that follows the LMTP naming
convention (t0_\emph{, t1_}, ..., t{K-1}_*).
}
\examples{
# create example data
df <- data.frame(
  t0_b = rnorm(100),
  t0_l = rnorm(100),
  t1_l = rnorm(100),
  t1_a = rbinom(100, 1, 0.5),
  t1_y = rnorm(100),
  t2_l = rnorm(100),
  t2_a = rbinom(100, 1, 0.5),
  t2_y = rnorm(100)
)
scm <- scm_from_df(df)
print(scm)
}
